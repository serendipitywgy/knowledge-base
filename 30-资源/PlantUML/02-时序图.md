# 02 · 时序图（Sequence）

← [[01-基础语法与通用命令]] · [[PlantUML从入门到精通|目录]] · 下一章 → [[03-用例图]]

官方：https://plantuml.com/zh/sequence-diagram

时序图按**时间顺序**描述对象之间如何发消息。接口文档、鉴权链路、支付回调——优先用它。

---

## 1. 最小例子

```plantuml
@startuml
Alice -> Bob: Authentication Request
Bob --> Alice: Authentication Response
Alice -> Bob: Another Request
Alice <-- Bob: Another Response
@enduml
```

| 符号           | 含义                  |
| ------------ | ------------------- |
| `->`         | 实线箭头（常表请求）          |
| `-->`        | 虚线箭头（常表返回）          |
| `<-` / `<--` | 反向写法，画面等价，可读性更好时可混用 |

参与者**不必预先声明**，首次出现即创建。

---

## 2. 声明参与者与外形

显式声明可控制**外形、颜色、从左到右顺序**：

```plantuml
@startuml
actor 用户 as User
boundary UI
control Ctrl
entity Ent
database DB
collections List
queue MQ
participant "订单服务" as Order #LightBlue

User -> UI: 点击下单
UI -> Ctrl: createOrder
Ctrl -> Order: RPC
Order -> DB: insert
Order -> MQ: 发事件
@enduml
```

关键字外形：

| 关键字           | 典型含义            |
| ------------- | --------------- |
| `actor`       | 人或外部角色          |
| `participant` | 通用参与者（矩形）       |
| `boundary`    | 边界（界面 / API 边缘） |
| `control`     | 控制类             |
| `entity`      | 实体              |
| `database`    | 数据库             |
| `collections` | 集合              |
| `queue`       | 队列              |

别名：`as`。长名字用引号：`participant "非常长的\n名字" as L`。

### 强制显示顺序：`order`

```plantuml
@startuml
participant 最后 order 30
participant 中间 order 20
participant 最前 order 10
最前 -> 中间: 1
中间 -> 最后: 2
@enduml
```

数字越小越靠左（默认声明顺序 = 显示顺序）。

---

## 3. 消息样式与颜色

```plantuml
@startuml
Bob ->x Alice: 丢失 / 失败
Bob ->> Alice: 异步细箭头
Bob -\ Alice: 半箭头
Bob ->o Alice: 圆头
Bob <-> Alice: 双向
Bob -[#red]> Alice: 红色紧急
@enduml
```

给自己发消息（内部处理）：

```plantuml
@startuml
Alice -> Alice: 校验参数\n写审计日志
@enduml
```

---

## 4. 生命线激活 / 返回

```plantuml
@startuml
用户 -> API: POST /orders
activate API
API -> DB: INSERT
activate DB
DB --> API: ok
deactivate DB
API --> 用户: 201
deactivate API
@enduml
```

简写：`API -> DB ++` 激活对方；`--` 停用；`return 文案` 生成返回消息（部分引擎支持）。

嵌套调用时务必成对 activate/deactivate，否则生命线「粘住」。

---

## 5. 组合片段（交互框）——必会

```plantuml
@startuml
Alice -> Bob: 认证请求

alt 成功
  Bob --> Alice: 接受
else 失败
  Bob --> Alice: 拒绝
  group 安全审计
    Alice -> Log: 记攻击日志
    loop 1000 次
      Alice -> Bob: 探测
    end
  end
else 需重试
  Bob --> Alice: 请重复
end

opt 已登录
  Alice -> Bob: 拉资料
end

par 并行通知
  Alice -> SMS: 短信
else
  Alice -> Mail: 邮件
end

break 熔断
  Alice -> Alice: 直接失败返回
end
@enduml
```

| 关键字 | 用途 |
|--------|------|
| `alt` / `else` / `end` | 互斥分支 |
| `opt` / `end` | 可选 |
| `loop` / `end` | 循环 |
| `par` / `else` / `end` | 并行 |
| `break` / `end` | 中断后续 |
| `critical` / `end` | 临界区 |
| `group 标题` / `end` | 自定义分组 |
| `group 标题 [次级标签]` | 分组副标题 |

可嵌套；`end` 不要漏。

---

## 6. 自动编号 autonumber

```plantuml
@startuml
autonumber
Alice -> Bob: 第一步
Bob --> Alice: 第二步

autonumber 15
Alice -> Bob: 从 15 继续

autonumber 40 10
Alice -> Bob: 40, 50, 60…

autonumber stop
Alice -> Bob: 不编号
autonumber resume
Alice -> Bob: 恢复编号
@enduml
```

格式串（Java DecimalFormat）：

```plantuml
@startuml
autonumber "<b>[000]"
Alice -> Bob: 请求
Bob --> Alice: 响应
@enduml
```

多级编号：`autonumber 1.1.1`，`autonumber inc A` / `inc B` 进位。  
注释里可用 `%autonumber%` 引用当前值。

---

## 7. 注释、分隔、引用、延迟、留白

```plantuml
@startuml
== 初始化 ==
Alice -> Bob: 握手
note right of Bob #FFAAAA: 校验证书
ref over Alice, Bob: 详见「TLS 子流程」文档

...5 分钟后...

== 业务 ==
Alice -> Bob: 下单
note across: 跨越全部参与者的说明
||| 
Alice -> Bob: 确认
||40||
Alice -> Bob: 结束
@enduml
```

| 写法 | 作用 |
|------|------|
| `== 标题 ==` | 逻辑阶段分隔 |
| `note left/right` | 跟在上一条消息旁 |
| `note left/right/over of X` | 相对参与者 |
| `note across` | 横跨全图 |
| `hnote` / `rnote` | 六边形 / 矩形备注 |
| `ref over` | 引用其它交互 |
| `...` / `...文字...` | 延迟 |
| `\|\|\|` / `\|\|45\|\|` | 垂直留白（像素） |

同一高度对齐多条 note：第二行前加 `/`。

---

## 8. 创建、页脚、分页、隐藏底栏

```plantuml
@startuml
title 支付主流程
header 内部资料
footer PlantUML 系列 · 02
hide footbox

用户 -> 支付: pay
newpage 退款子流程
用户 -> 支付: refund
@enduml
```

`hide footbox` 去掉参与者底部名字条，图更紧凑。

---

## 9. 完整实战：支付（建议收藏）

```plantuml
@startuml
title 用户支付流程
autonumber
actor 用户
participant "前端" as F
participant "订单服务" as O
participant "支付服务" as P
participant "第三方支付" as T
database MySQL
database Redis

用户 -> F: 点击支付
activate F

F -> O: 创建订单
activate O
O -> MySQL: 保存订单
O -> Redis: 缓存订单
O --> F: 订单号
deactivate O

F -> P: 发起支付
activate P
P -> T: 调起收银台
activate T

alt 支付成功
  T --> P: 成功回调
  P -> O: 更新已支付
  P --> F: 成功
  F --> 用户: 成功页
else 支付失败
  T --> P: 失败
  P --> F: 失败原因
  F --> 用户: 失败页
end

deactivate T
deactivate P
deactivate F
@enduml
```

---

## 10. 文本与布局微调

```plantuml
@startuml
skinparam sequenceMessageAlign right
skinparam responseMessageBelowArrow true
skinparam maxMessageSize 50
Alice -> Bob: Request
Bob --> Alice: Response with long text
@enduml
```

过长中文换行优先手动 `\n`。参与者过多（>6～8）：**拆图**比硬挤更好。

Teoz 引擎（进阶）：`!pragma teoz true`，支持时长条、锚点等，见官网。

---

## 11. 速查

```
actor / participant / database / queue …
A -> B: msg     A --> B: return
activate X / deactivate X
alt / else / end   opt / loop / par / break / group
autonumber [start [step]] [ "format" ]
== 阶段 ==    ...延迟...    |||
note right of X    note across    ref over A,B
title / header / footer / hide footbox / newpage
```

## 12. 练习

1. 画「短信登录」：发码 → 校验 → 成功/失败 `alt`。  
```plantuml
 @startuml
  title 短信登录
  actor 用户
  participant 前端 as FE
  participant 认证服务 as Auth
  participant 短信网关 as SMS
  用户 -> FE: 输入手机号
  FE -> Auth: 请求发送验证码
  Auth -> SMS: 发短信
  SMS --> Auth: 已发送
  Auth --> FE: 提示查收
  用户 -> FE: 输入验证码
  FE -> Auth: 校验验证码
  alt 验证码正确
    Auth --> FE: 登录成功 + token
    FE --> 用户: 进入首页
  else 验证码错误/过期
    Auth --> FE: 登录失败
    FE --> 用户: 提示重试
  end
  @enduml

@enduml
```
1. 给所有消息加上 `autonumber`，失败分支用 `group` 标出。  
2. 把本库 [[上下面-三段式]] 改造成带 `database` 的三段调用。

---

下一章 → [[03-用例图]]
